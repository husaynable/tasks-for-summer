import { InjectionToken } from '@angular/core';

export const FED_CATS_COUNT = new InjectionToken<number>('FED_CATS_COUNT');

export const COUNTER_COUNT = new InjectionToken<number>('COUNTER_COUNT');
